{
  "id": "019d440d-f81f-75e2-9415-ab272ed158d5",
  "content": ";\n\n        // Account for the line content + the \\n delimiter.\n        let line_bytes = line.len() as u64 + 1; // +1 for \\n\n        byte_pos += line_bytes;\n\n        // Skip empty lines.\n        if line.is_empty() {\n            result.new_offset = byte_pos;\n            continue;\n        }\n\n        // Skip oversized lines.\n        if line.len() > MAX_LINE_BYTES {\n            tracing::warn!(\n                path = %path.display(),\n                line_len = line.len(),\n                \"skipping oversized JSONL line\"\n            );\n            result.new_offset = byte_pos;\n            continue;\n        }\n\n        // Extract event type for the broadcast.\n        if let Ok(v) = serde_json::from_str::<serde_json::Value>(&line)\n            && let Some(t) = v.get(\"type\").and_then(|t| t.as_str())\n        {\n            result.last_event_type = Some(t.to_string());\n        }\n\n        result.lines.push(line);\n        result.new_offset = byte_pos;\n    }\n\n    // If file doesn't end with \\n (partial write), we've already stopped at\n    // the last complete line because BufReader::lines() only yields complete\n    // lines (terminated by \\n or EOF). However, an EOF-terminated line without\n    // \\n means the writer may still be appending. We check: if our computed\n    // byte_pos exceeds file_len, clamp to file_len.\n    if result.new_offset > file_len {\n        result.new_offset = file_len;\n    }\n\n    result\n}",
  "source_version": "crates/corvia-server/src/dashboard/session_watcher.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.423544785Z",
  "valid_from": "2026-03-31T13:20:59.423544785Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-server/src/dashboard/session_watcher.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 422,
    "end_line": 457,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}