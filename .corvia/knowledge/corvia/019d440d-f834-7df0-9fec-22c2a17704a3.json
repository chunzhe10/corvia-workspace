{
  "id": "019d440d-f834-7df0-9fec-22c2a17704a3",
  "content": "pub fn blocked_path_match(pattern: &str, path: &str) -> bool {\n    if let Some(prefix) = pattern.strip_suffix('*') {\n        path.starts_with(prefix)\n    } else {\n        path == pattern\n    }\n}\npub async fn wire_pipeline_relations(\n    relations: &[ChunkRelation],\n    processed: &[ProcessedChunk],\n    stored_ids: &[uuid::Uuid],\n    graph: &dyn GraphStore,\n) -> usize {\n    // Build file index for fast lookup: source_file -> Vec<(chunk_index, &ProcessedChunk)>\n    let mut file_index: HashMap<&str, Vec<(usize, &ProcessedChunk)>> =\n        HashMap::with_capacity(processed.len());\n    for (i, pc) in processed.iter().enumerate() {\n        file_index\n            .entry(pc.metadata.source_file.as_str())\n            .or_default()\n            .push((i, pc));\n    }\n\n    // Build source index for O(1) source chunk lookup: (source_file, start_line) -> chunk_index\n    // Uses or_insert to match first-match semantics of the old Iterator::position() scan.\n    let mut source_index: HashMap<(&str, u32), usize> = HashMap::with_capacity(processed.len());\n    for (i, pc) in processed.iter().enumerate() {\n        source_index\n            .entry((pc.metadata.source_file.as_str(), pc.start_line))\n            .or_insert(i);\n    }\n",
  "source_version": "crates/corvia-kernel/src/ingest.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.444327172Z",
  "valid_from": "2026-03-31T13:20:59.444327172Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-kernel/src/ingest.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 259,
    "end_line": 283,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}