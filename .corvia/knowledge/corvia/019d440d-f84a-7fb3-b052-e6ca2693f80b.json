{
  "id": "019d440d-f84a-7fb3-b052-e6ca2693f80b",
  "content": "et time_trigger = now.saturating_sub(last) >= self.flush_interval_secs;\n        let size_trigger = self.pending.len() >= self.max_pending;\n\n        if !time_trigger && !size_trigger {\n            return 0;\n        }\n\n        // CAS to claim the flush lock.\n        if self.flushing.compare_exchange(false, true, Ordering::AcqRel, Ordering::Relaxed).is_err() {\n            return 0;\n        }\n\n        let flushed = self.flush_inner(db);\n\n        self.last_flush.store(epoch_secs(), Ordering::Relaxed);\n        self.flushing.store(false, Ordering::Release);\n\n        flushed\n    }\n\n    /// Synchronous flush of all pending events. Called from LiteStore::Drop.\n    pub fn flush_sync(&self, db: &Arc<Database>) -> usize {\n        if self.pending.is_empty() {\n            return 0;\n        }\n        self.flush_inner(db)\n    }\n\n    /// Number of pending (unflushed) entries.\n    pub fn pending_count(&self) -> usize {\n        self.pending.len()\n    }\n\n    /// Internal flush implementation shared by try_flush and flush_sync.\n    fn flush_inner(&self, db: &Arc<Database>) -> usize {\n        use crate::lite_store::ENTRIES;\n        use corvia_common::types::KnowledgeEntry;\n        use redb::ReadableTable;\n\n        // Drain all pending entries atomically (per-key).\n        let batch: Vec<(uuid::Uuid, BufferedAccess)> = self\n            .pending\n            .iter()\n            .map(|r| (*r.key(), r.value().clone()))\n            .collect();\n\n        if batch.is_empty() {\n            return 0;\n        }\n\n        // Remove drained entries from the map.\n        for (id, _) in &batch {\n            self.pending.remove(id);\n        }\n\n        let count = batch.len();\n\n        let write_txn = match db.begin_write() {\n            Ok(txn) => txn,\n            Err(e) => {\n                warn!(error = %e, \"AccessBuffer: failed to begin write txn for flush\");",
  "source_version": "docs/superpowers/plans/2026-03-28-buffered-access-tracking-plan.md",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.466497461Z",
  "valid_from": "2026-03-31T13:20:59.466497461Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "docs/superpowers/plans/2026-03-28-buffered-access-tracking-plan.md",
    "language": "markdown",
    "chunk_type": "heading_section",
    "start_line": 157,
    "end_line": 209,
    "content_role": "plan",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "decisional",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}