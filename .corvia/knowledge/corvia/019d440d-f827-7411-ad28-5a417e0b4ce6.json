{
  "id": "019d440d-f827-7411-ad28-5a417e0b4ce6",
  "content": "herit it.\n    // The guard is intentionally dropped — tracing-opentelemetry captures the\n    // current OTel context at span creation time, so child spans created\n    // immediately after will pick up the parent.\n    parent_cx.attach();\n    Ok(request)\n}\nasync fn main() -> Result<(), Box<dyn std::error::Error>> {\n    let telemetry_config = TelemetryConfig {\n        service_name: \"corvia-inference\".into(),\n        ..Default::default()\n    };\n    let _telemetry_guard = corvia_telemetry::init_telemetry(&telemetry_config)?;\n\n    let cli = Cli::parse();\n\n    match cli.command {\n        Commands::Serve { port, health_probe_interval, health_probe_drift_threshold } => {\n            let addr = format!(\"0.0.0.0:{port}\").parse()?;\n\n            // Probe GPU capabilities once at startup (wrapped for runtime re-probing)\n            let gpu = backend::GpuCapabilities::probe();\n            tracing::info!(\n                cuda = gpu.cuda_available,\n                openvino = gpu.openvino_available,\n                \"GPU capabilities\"\n            );\n            let gpu = std::sync::Arc::new(std::sync::RwLock::new(gpu));\n\n            let embed_svc = embedding_service::EmbeddingServiceImpl::new();\n            let chat_svc = chat_service::ChatServiceImpl::new();\n            let probe_state = probe::new_shared_probe_state();\n            let model_mgr = model_manager::ModelManagerService::new(\n                embed_svc.clone(),\n                chat_svc.clone(),\n                gpu,\n                probe_state,\n                model_manager::ProbeConfig {\n                    interval_secs: health_probe_interval,\n                    drift_threshold_pct: health_probe_drift_threshold,\n                },\n            );\n\n            tracing::info!(port, \"inference_server_starting\");\n\n            Server::builder()\n                .add_service(ModelManagerServer::new(model_mgr))\n                .add_service(EmbeddingServiceServer::with_interceptor(embed_svc, accept_trace))\n                .add_service(ChatServiceServer::with_interceptor(chat_svc, accept_trace))\n                .serve(addr)\n                .await?;\n        }\n    }\n\n    Ok(())\n}",
  "source_version": "crates/corvia-inference/src/main.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.431004405Z",
  "valid_from": "2026-03-31T13:20:59.431004405Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-inference/src/main.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 59,
    "end_line": 107,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}