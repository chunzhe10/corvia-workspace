{
  "id": "019d440d-f840-7443-8b0a-1c3503def4e9",
  "content": "### Task 1: K-means and Silhouette Core\n\n            let nearest = nearest_centroid(point, &centroids);\n            if nearest != assignments[i] {\n                assignments[i] = nearest;\n                changed = true;\n            }\n        }\n        if !changed {\n            break;\n        }\n\n        // Update step\n        let mut sums = vec![vec![0.0f32; dim]; k];\n        let mut counts = vec![0usize; k];\n        for (i, point) in data.iter().enumerate() {\n            let c = assignments[i];\n            counts[c] += 1;\n            for (j, val) in point.iter().enumerate() {\n                sums[c][j] += val;\n            }\n        }\n        for c in 0..k {\n            if counts[c] > 0 {\n                for j in 0..dim {\n                    centroids[c][j] = sums[c][j] / counts[c] as f32;\n                }\n            }\n        }\n    }\n    assignments\n}\n\nfn kmeans_pp_init(data: &[Vec<f32>], k: usize) -> Vec<Vec<f32>> {\n    use rand::Rng;\n    let mut rng = rand::thread_rng();\n    let mut centroids = vec![data[rng.gen_range(0..data.len())].clone()];\n\n    for _ in 1..k {\n        let distances: Vec<f32> = data.iter().map(|p| {\n            centroids.iter().map(|c| euclidean_dist_sq(p, c)).fold(f32::MAX, f32::min)\n        }).collect();\n        let total: f32 = distances.iter().sum();\n        let threshold = rng.gen::<f32>() * total;\n        let mut cumulative = 0.0;\n        for (i, &d) in distances.iter().enumerate() {\n            cumulative += d;\n            if cumulative >= threshold {\n                centroids.push(data[i].clone());\n                break;\n            }\n        }\n    }\n    centroids\n}\n\nfn nearest_centroid(point: &[f32], centroids: &[Vec<f32>]) -> usize {\n    centroids.iter().enumerate()\n        .min_by(|(_, a), (_, b)| euclidean_dist_sq(point, a).partial_cmp(&euclidean_dist_sq(point, b)).unwrap())\n        .map(|(i, _)| i)\n        .unwrap()\n}\n\nfn euclidean_dist_sq(a: &[f32], b: &[f32]) -> f32 {\n    a.iter().zip(b.iter()).map(|(x, y)| (x - y) * (x - y)).sum()",
  "source_version": "docs/rfcs/2026-03-13-dashboard-ux-overhaul-plan.md",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.456231988Z",
  "valid_from": "2026-03-31T13:20:59.456231988Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "docs/rfcs/2026-03-13-dashboard-ux-overhaul-plan.md",
    "language": "markdown",
    "chunk_type": "heading_section",
    "start_line": 152,
    "end_line": 214,
    "content_role": "design",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "decisional",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}