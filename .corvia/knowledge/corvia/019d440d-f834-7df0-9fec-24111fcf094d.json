{
  "id": "019d440d-f834-7df0-9fec-24111fcf094d",
  "content": "rr(|e| anyhow::anyhow!(e))?;\n            } else {\n                progress.log(\n                    \"\\n  Skipping workspace docs — no suitable adapter found \\\n                     (install corvia-adapter-basic)\"\n                );\n            }\n        }\n    }\n\n    // --- Phase 3: Claude Code session history (only on full ingest) ---\n    if repo_filter.is_none() {\n        let session_scope = config\n            .scope\n            .as_ref()\n            .and_then(|scopes| scopes.iter().find(|s| s.id == USER_HISTORY_SCOPE));\n\n        if session_scope.is_some() {\n            // Acquire session lock if provided (prevents race with /v1/ingest/sessions)\n            let _session_guard = if let Some(lock) = session_lock {\n                Some(lock.lock().await)\n            } else {\n                None\n            };\n\n            let adapter_info = discovered.iter().find(|a| a.metadata.domain == CLAUDE_SESSIONS_ADAPTER);\n            if let Some(adapter_info) = adapter_info {\n                let home = std::env::var(\"HOME\").unwrap_or_else(|_| \"/root\".into());\n                let sessions_dir = PathBuf::from(home).join(\".claude\").join(\"sessions\");\n\n                if sessions_dir.is_dir() {",
  "source_version": "crates/corvia-kernel/src/ingest.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.444527482Z",
  "valid_from": "2026-03-31T13:20:59.444527482Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-kernel/src/ingest.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 946,
    "end_line": 968,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}