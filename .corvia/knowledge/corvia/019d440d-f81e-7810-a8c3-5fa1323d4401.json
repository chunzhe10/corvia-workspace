{
  "id": "019d440d-f81e-7810-a8c3-5fa1323d4401",
  "content": "cate a string at a char boundary, appending \"…\" if shortened.\n    fn truncate_str(s: &str, max: usize) -> String {\n        if s.len() <= max { return s.to_string(); }\n        let end = s.floor_char_boundary(max);\n        format!(\"{}…\", &s[..end])\n    }\n\n    let nodes: Vec<serde_json::Value> = node_ids\n        .iter()\n        .map(|id| {\n            let entry = entry_map.get(id);\n            // Prefer source_file for the label, fall back to content preview\n            let label = entry\n                .and_then(|e| e.metadata.source_file.as_deref().map(|s| s.to_string()))\n                .or_else(|| entry.map(|e| truncate_str(&e.content, 80)))\n                .unwrap_or_else(|| truncate_str(&id.to_string(), 8));\n            let preview = entry\n                .map(|e| truncate_str(&e.content, 200))\n                .unwrap_or_default();\n            // Derive cluster group from source_file path\n            let group = entry\n                .and_then(|e| e.metadata.source_file.as_deref())\n                .map(|sf| {\n                    let parts: Vec<&str> = sf.split('/').collect();\n                    if (parts.first() == Some(&\"crates\") || parts.first() == Some(&\"adapters\"))\n                        && parts.len() >= 2\n                    {\n                        parts[1].to_string()\n                    } else if parts.first() == Some(&\"docs\") {\n                        \"docs\".to_string()\n                    } else if parts.first() == Some(&\"tests\") {",
  "source_version": "crates/corvia-server/src/dashboard/mod.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.422196032Z",
  "valid_from": "2026-03-31T13:20:59.422196032Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-server/src/dashboard/mod.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 480,
    "end_line": 504,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}