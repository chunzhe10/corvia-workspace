{
  "id": "019d440d-f841-7b10-9b8b-0b400970f4f5",
  "content": "rates/corvia-kernel/src/lib.rs` after line 84 (`pub mod chunking_strategy;`):\n```rust\npub mod chunking_fallback;\n```\n\nRun: `cargo test -p corvia-kernel chunking_fallback -- --nocapture`\nExpected: FAIL (todo!() panics)\n\n**Step 3: Implement FallbackChunker**\n\nReplace the `chunk()` method with recursive separator cascade:\n\n```rust\nimpl ChunkingStrategy for FallbackChunker {\n    fn name(&self) -> &str {\n        \"fallback\"\n    }\n\n    fn supported_extensions(&self) -> &[&str] {\n        &[] // fallback matches everything via FormatRegistry, not by extension\n    }\n\n    fn chunk(&self, source: &str, meta: &SourceMetadata) -> Result<Vec<RawChunk>> {\n        if source.is_empty() {\n            return Ok(vec![]);\n        }\n\n        let max_chars = self.max_tokens * 4; // chars/4 heuristic inverse\n        let separators = [\"\\n\\n\", \"\\n\", \" \"];\n\n        let pieces = recursive_split(source, &separators, max_chars);\n\n        let mut chunks = Vec::new();\n        let mut char_offset = 0;\n\n        for piece in pieces {\n            if piece.is_empty() {\n                continue;\n            }\n            let start_line = source[..char_offset].matches('\\n').count() as u32 + 1;\n            let end_line = start_line + piece.matches('\\n').count() as u32;\n            chunks.push(RawChunk {\n                content: piece.to_string(),\n                chunk_type: \"text\".into(),\n                start_line,\n                end_line,\n                metadata: ChunkMetadata {\n                    source_file: meta.file_path.clone(),\n                    language: meta.language.clone(),\n                    ..Default::default()\n                },\n            });\n            // Advance offset past this piece + the separator that was consumed\n            char_offset += piece.len();\n            // Skip past the separator between this piece and the next\n            if char_offset < source.len() {\n                for sep in &separators {\n                    if source[char_offset..].starts_with(sep) {\n                        char_offset += sep.len();\n                        break;\n                    }\n                }\n            }\n        }\n\n        Ok(chunks)\n    }\n}\n",
  "source_version": "docs/rfcs/2026-03-02-m3.3-embedding-chunking-impl.md",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.457236116Z",
  "valid_from": "2026-03-31T13:20:59.457236116Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "docs/rfcs/2026-03-02-m3.3-embedding-chunking-impl.md",
    "language": "markdown",
    "chunk_type": "heading_section",
    "start_line": 147,
    "end_line": 206,
    "content_role": "design",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "decisional",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}