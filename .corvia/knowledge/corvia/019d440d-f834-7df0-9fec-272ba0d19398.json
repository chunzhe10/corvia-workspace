{
  "id": "019d440d-f834-7df0-9fec-272ba0d19398",
  "content": "cpath, hpath.as_str()));\n            }\n        }\n        best_match.map(|(cpath, hpath)| {\n            let hpath = hpath.trim_end_matches('/');\n            let suffix = &container_path[cpath.len()..];\n            format!(\"{hpath}{suffix}\")\n        })\n    }\npub fn select_network(\n    networks: &HashMap<String, NetworkInfo>,\n    config_override: Option<&str>,\n) -> Result<String> {\n    if let Some(net) = config_override {\n        if networks.contains_key(net) {\n            return Ok(net.to_string());\n        }\n        return Err(CorviaError::Config(format!(\n            \"Configured network '{}' not found. Available: {:?}\",\n            net,\n            networks.keys().collect::<Vec<_>>()\n        )));\n    }\n\n    let candidates: Vec<_> = networks\n        .keys()\n        .filter(|n| !matches!(n.as_str(), \"bridge\" | \"host\" | \"none\"))\n        .collect();\n\n    match candidates.len() {\n        0 => Err(CorviaError::Config(\n            \"No user-defined Docker network found. \\\n             Set [workspace.spokes] network in corvia.toml.\"\n                .into(),\n        )),\n        1 => Ok(candidates[0].clone()),\n        _ => {\n            if let Some(net) = candidates.iter().find(|n| n.contains(\"devcontainer\")) {\n                return Ok((*net).clone());\n            }\n            Err(CorviaError::Config(format!(\n                \"Multiple networks found: {:?}. Set [workspace.spokes] network in corvia.toml.\",\n                candidates\n            )))\n        }\n    }\n}",
  "source_version": "crates/corvia-kernel/src/spoke.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.444923136Z",
  "valid_from": "2026-03-31T13:20:59.444923136Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-kernel/src/spoke.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 145,
    "end_line": 182,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}