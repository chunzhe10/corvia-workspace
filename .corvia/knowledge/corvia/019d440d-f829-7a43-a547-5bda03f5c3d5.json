{
  "id": "019d440d-f829-7a43-a547-5bda03f5c3d5",
  "content": "    };\n    let confidence: Option<f64> = row.try_get(\"confidence\").ok().flatten();\n    let tier_str: String = row.try_get(\"tier\").unwrap_or_else(|_| \"hot\".into());\n    let tier = match tier_str.as_str() {\n        \"warm\" => corvia_common::types::Tier::Warm,\n        \"cold\" => corvia_common::types::Tier::Cold,\n        \"forgotten\" => corvia_common::types::Tier::Forgotten,\n        _ => corvia_common::types::Tier::Hot,\n    };\n    let tier_changed_at: Option<chrono::DateTime<chrono::Utc>> =\n        row.try_get(\"tier_changed_at\").ok().flatten();\n    let retention_score_f64: Option<f64> = row.try_get(\"retention_score\").ok().flatten();\n    let pinned: bool = row.try_get(\"pinned\").unwrap_or(false);\n    let pin = if pinned {\n        let pinned_by: Option<String> = row.try_get(\"pinned_by\").ok().flatten();\n        let pinned_at: Option<chrono::DateTime<chrono::Utc>> = row.try_get(\"pinned_at\").ok().flatten();\n        match (pinned_by, pinned_at) {\n            (Some(by), Some(at)) => Some(corvia_common::types::PinInfo { by, at }),\n            _ => {\n                warn!(\"Entry has pinned=true but missing pin metadata, treating as unpinned\");\n                None\n            }\n        }\n    } else {\n        None\n    };\n\n    Some(KnowledgeEntry {\n        id,\n        content,\n        source_version,\n        scope_id,\n        workstream,\n        recorded_at,\n        valid_from,\n        valid_to,\n        superseded_by,\n        embedding,\n        metadata,\n        agent_id,\n        session_id,\n        entry_status,\n        memory_type,\n        confidence: confidence.map(|c| c as f32),\n        last_accessed,\n        access_count: access_count as u32,\n        tier,\n        tier_changed_at,\n        retention_score: retention_score_f64.map(|r| r as f32),\n        pin,\n    })\n}",
  "source_version": "crates/corvia-kernel/src/postgres_store.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.433677555Z",
  "valid_from": "2026-03-31T13:20:59.433677555Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-kernel/src/postgres_store.rs",
    "language": "rs",
    "chunk_type": "function_item",
    "start_line": 197,
    "end_line": 243,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}