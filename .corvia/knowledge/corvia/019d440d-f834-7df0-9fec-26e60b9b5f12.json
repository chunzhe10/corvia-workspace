{
  "id": "019d440d-f834-7df0-9fec-26e60b9b5f12",
  "content": "  for (name, endpoint) in ns {\n                networks.insert(\n                    name,\n                    NetworkInfo {\n                        network_id: endpoint.network_id.unwrap_or_default(),\n                    },\n                );\n            }\n        }\n\n        Ok(Self {\n            container_name: hostname,\n            networks,\n            host_mounts,\n        })\n    }\n\n    /// Translate a container path to a host path using the mount table.\n    /// Returns an error with diagnostic info if the path cannot be resolved.\n    pub fn host_path_or_err(&self, container_path: &str) -> Result<String> {\n        self.host_path(container_path).ok_or_else(|| {\n            let mounts: Vec<_> = self.host_mounts.keys().collect();\n            CorviaError::Config(format!(\n                \"Cannot resolve host path for '{}'. \\\n                 Hub mount table: {:?}. \\\n                 Ensure the devcontainer mounts cover this path.\",\n                container_path, mounts\n            ))\n        })\n    }\n\n    /// Translate a container path to a host path. Returns None if no mount covers this path.\n    ///\n    /// Enforces path boundaries: a mount for `/data` will NOT match `/datafiles`.\n    /// The container path must either equal the mount path or have a `/` separator.\n    pub fn host_path(&self, container_path: &str) -> Option<String> {\n        let mut best_match: Option<(&str, &str)> = None;\n        for (cpath, hpath) in &self.host_mounts {\n            let cpath = cpath.trim_end_matches('/');\n            // Enforce path boundary: exact match or followed by '/'\n            let is_match = container_path == cpath\n                || container_path.starts_with(&format!(\"{cpath}/\"));\n            if is_match && best_match.is_none_or(|(bp, _)| cpath.len() > bp.len()) {\n                best_match = Some((cpath, hpath.as_str()));\n            }\n        }\n        best_match.map(|(cpath, hpath)| {\n            let hpath = hpath.trim_end_matches('/');\n            let suffix = &container_path[cpath.len()..];\n            format!(\"{hpath}{suffix}\")\n        })\n    }\n}",
  "source_version": "crates/corvia-kernel/src/spoke.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.444883357Z",
  "valid_from": "2026-03-31T13:20:59.444883357Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-kernel/src/spoke.rs",
    "language": "rs",
    "chunk_type": "impl_item",
    "start_line": 91,
    "end_line": 135,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}