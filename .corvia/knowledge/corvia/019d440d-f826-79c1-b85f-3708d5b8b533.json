{
  "id": "019d440d-f826-79c1-b85f-3708d5b8b533",
  "content": "           backend = %attempt_backend,\n                        error = %e,\n                        \"ONNX session creation failed for backend\"\n                    );\n                    // Continue to next fallback\n                }\n            }\n        }\n\n        let engine = engine.ok_or_else(|| {\n            Status::internal(\"Model load failed: all backends (including CPU) failed\")\n        })?;\n\n        self.models\n            .lock()\n            .map_err(|e| Status::internal(format!(\"Lock poisoned: {e}\")))?\n            .insert(\n                name_owned.clone(),\n                LoadedModel {\n                    engine,\n                    variant: model_enum,\n                    backend,\n                },\n            );\n        tracing::info!(model = %name_owned, \"Embedding model loaded — running canary test...\");\n\n        // Run canary embedding to verify the execution provider actually works.\n        // This catches silent CPU fallback, broken EP libraries, and other runtime\n        // issues that only surface when inference is attempted.\n        let canary_models = self.models.clone();\n        let canary_name = name_owned.clone();\n        let canary_result = tokio::task::spawn_blocking(move || {\n            let canary_start = std::time::Instant::now();\n            let mut guard = canary_models\n                .lock()\n                .map_err(|e| format!(\"Lock poisoned: {e}\"))?;\n            let loaded = guard\n                .get_mut(&canary_name)\n                .ok_or_else(|| format!(\"Model '{canary_name}' disappeared after insert\"))?;\n\n            let expected_dims = fastembed::TextEmbedding::get_model_info(&loaded.variant)\n                .map(|info| info.dim)\n                .unwrap_or(0);\n\n            let result = loaded",
  "source_version": "crates/corvia-inference/src/embedding_service.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.430496236Z",
  "valid_from": "2026-03-31T13:20:59.430496236Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-inference/src/embedding_service.rs",
    "language": "rs",
    "chunk_type": "impl_item",
    "start_line": 272,
    "end_line": 307,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}