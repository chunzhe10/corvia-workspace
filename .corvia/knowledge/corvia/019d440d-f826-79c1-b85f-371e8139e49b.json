{
  "id": "019d440d-f826-79c1-b85f-371e8139e49b",
  "content": "|| format!(\"Model '{canary_name}' disappeared after insert\"))?;\n\n            let expected_dims = fastembed::TextEmbedding::get_model_info(&loaded.variant)\n                .map(|info| info.dim)\n                .unwrap_or(0);\n\n            let result = loaded\n                .engine\n                .embed(vec![\"corvia canary test\"], None)\n                .map_err(|e| format!(\"Canary embed failed: {e}\"))?;\n\n            let embedding = result\n                .into_iter()\n                .next()\n                .ok_or_else(|| \"Canary returned empty result\".to_string())?;\n\n            let latency = canary_start.elapsed();\n            Ok::<_, String>((embedding, expected_dims, latency))\n        })\n        .await\n        .map_err(|e| Status::internal(format!(\"Canary spawn failed: {e}\")));\n\n        match canary_result {\n            Ok(Ok((embedding, expected_dims, latency))) => {\n                // Verify the embedding is non-zero (valid)\n                let is_nonzero = embedding.iter().any(|&v| v != 0.0);\n                let actual_dims = embedding.len();\n\n                if !is_nonzero {\n                    tracing::warn!(\n                        model = %name_owned,\n                        \"Canary embedding is all zeros — EP may not be working correctly\"\n                    );\n                }\n\n                if expected_dims > 0 && actual_dims != expected_dims {\n                    tracing::warn!(\n                        model = %name_owned,\n                        expected_dims,\n                        actual_dims,\n                        \"Canary embedding dimensions mismatch\"\n                    );\n                }\n",
  "source_version": "crates/corvia-inference/src/embedding_service.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.430507276Z",
  "valid_from": "2026-03-31T13:20:59.430507276Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-inference/src/embedding_service.rs",
    "language": "rs",
    "chunk_type": "impl_item",
    "start_line": 308,
    "end_line": 344,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}