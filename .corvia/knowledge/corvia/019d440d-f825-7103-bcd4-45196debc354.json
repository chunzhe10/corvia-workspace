{
  "id": "019d440d-f825-7103-bcd4-45196debc354",
  "content": "us::internal(format!(\"GPU lock poisoned: {e}\")))?;\n            *gpu = new_gpu;\n        }\n\n        // Snapshot currently loaded models (optionally filtered by name)\n        let snapshot: Vec<ModelEntry> = {\n            let models = self.models.read().await;\n            models.values()\n                .filter(|m| m.loaded)\n                .filter(|m| req.name.is_empty() || m.name == req.name)\n                .cloned()\n                .collect()\n        };\n\n        if snapshot.is_empty() {\n            return Ok(Response::new(ReloadModelsResponse {\n                success: true,\n                error: String::new(),\n                results: vec![],\n            }));\n        }\n\n        let mut results = Vec::new();\n        let mut all_success = true;\n\n        for entry in &snapshot {\n            let model_type = match entry.model_type.as_str() {\n                \"embedding\" => ModelType::Embedding,\n                \"chat\" => ModelType::Chat,\n                _ => {\n                    results.push(ModelReloadResult {\n                        name: entry.name.clone(),\n                        model_type: entry.model_type.clone(),\n                        success: false,\n                        error: format!(\"Unknown model_type: '{}'\", entry.model_type),\n                        actual_device: String::new(),\n                        actual_backend: String::new(),\n                    });\n                    all_success = false;\n                    continue;\n                }\n            };\n\n            // Use embedding_backend override for embedding models when set.\n            let effective_backend = if model_type == ModelType::Embedding && !req.embedding_backend.is_empty() {\n                &req.embedding_backend\n            } else {\n                &req.backend\n            };\n\n            let resolved = {\n                let gpu = self.gpu.read().map_err(|e| Status::internal(format!(\"GPU lock poisoned: {e}\")))?;\n                match backend::resolve_backend(&req.device, effective_backend, model_type, &gpu) {\n                    Ok(r) => r,\n                    Err(e) => {\n                        results.push(ModelReloadResult {",
  "source_version": "crates/corvia-inference/src/model_manager.rs",
  "scope_id": "corvia",
  "workstream": "kernel",
  "recorded_at": "2026-03-31T13:20:59.429969154Z",
  "valid_from": "2026-03-31T13:20:59.429969154Z",
  "valid_to": null,
  "superseded_by": null,
  "metadata": {
    "source_file": "crates/corvia-inference/src/model_manager.rs",
    "language": "rs",
    "chunk_type": "impl_item",
    "start_line": 325,
    "end_line": 373,
    "content_role": "code",
    "source_origin": "repo:corvia"
  },
  "agent_id": null,
  "session_id": null,
  "entry_status": "Merged",
  "memory_type": "structural",
  "confidence": null,
  "last_accessed": null,
  "access_count": 0,
  "tier": "hot",
  "tier_changed_at": null,
  "retention_score": null,
  "pin": null
}